/**
 Express.js – express.Router() (Easy & Real-world Explanation)

--------------------------------------------------
🔹 Router kya hota hai? (Simple words me)

- express.Router() ek mini express app jaisa hota hai
- Iska use routes ko alag–alag file me todne ke liye hota hai
- Taaki code clean, readable aur manageable rahe

👉 Simple line me:
Router ka use karke hum "routes ko group" kar dete hain

--------------------------------------------------
🔹 Router ke bina problem kya hoti hai? (IMPORTANT)

Agar router use nahi karte, to:
- Saare routes ek hi file (server.js / app.js) me aa jate hain
- File bahut lambi ho jati hai
- Samajhna mushkil ho jata hai
- Maintain karna hard ho jata hai
- Real-world projects me ye BAD PRACTICE hai

--------------------------------------------------
🔹 Router ke bina example (Problem)

import express from "express";
const app = express();

app.get("/api/users", (req, res) => {
  res.send("Get all users");
});

app.post("/api/users", (req, res) => {
  res.send("Create user");
});

app.get("/api/products", (req, res) => {
  res.send("Get all products");
});

app.post("/api/products", (req, res) => {
  res.send("Create product");
});

app.listen(3000);

❌ Problem:
- Users aur Products ke routes mix ho gaye
- Jaise-jaise app badhega, file aur gandi hoti jayegi
- Kisi ek feature ke routes dhoondhna mushkil ho jayega

--------------------------------------------------
🔹 Router ke bina dry run (Samajhne ke liye)

Request:
GET /api/users

Flow:
1) Express server.js file ko top se check karega
2) Pehle matching route milega
3) app.get("/api/users") execute ho jayega
4) Response send ho jayega

👉 Chal to raha hai, lekin structure kharab hai

--------------------------------------------------
🔹 Router use karne ka idea (Solution)

- Users ke routes → users.routes.js
- Products ke routes → products.routes.js
- Main file sirf router ko mount karegi

--------------------------------------------------
🔹 Router ke saath example (Clean & Professional)

users.routes.js
----------------
import express from "express";
const router = express.Router();

router.get("/", (req, res) => {
  res.send("Get all users");
});

router.post("/", (req, res) => {
  res.send("Create user");
});

export default router;

--------------------------------------------------

products.routes.js
------------------
import express from "express";
const router = express.Router();

router.get("/", (req, res) => {
  res.send("Get all products");
});

router.post("/", (req, res) => {
  res.send("Create product");
});

export default router;

--------------------------------------------------

server.js
---------
import express from "express";
import userRouter from "./users.routes.js";
import productRouter from "./products.routes.js";

const app = express();

app.use("/api/users", userRouter);
app.use("/api/products", productRouter);

app.listen(3000);

--------------------------------------------------
🔹 Router ke saath dry run (IMPORTANT)

Request:
GET /api/users

Flow:
1) Express server.js me request receive karega
2) app.use("/api/users", userRouter) match hoga
3) Control userRouter ko de diya jayega
4) Router ke andar "/" path check hoga
5) router.get("/") match ho jayega
6) Response send ho jayega

Output:
Get all users

--------------------------------------------------
🔹 Router ke saath path ka magic (IMPORTANT)

server.js:
app.use("/api/users", userRouter);

users.routes.js:
router.get("/", handler);

👉 Actual URL ban jata hai:
GET /api/users

Isliye router file me "/api/users" likhne ki zaroorat nahi hoti

--------------------------------------------------
🔹 Router se kya problem solve hoti hai?

✅ Code clean ho jata hai  
✅ Har feature ke routes alag ho jate hain  
✅ Large project manageable ho jata hai  
✅ Team work easy ho jata hai  
✅ Debugging easy ho jati hai  

--------------------------------------------------
🔹 Router + middleware (Real-world use)

Example:
router.use(authMiddleware);

Iska matlab:
- Is router ke saare routes pe auth middleware lagega
- Baar-baar likhne ki zaroorat nahi

--------------------------------------------------
🔹 Interview GOLD line

express.Router() ek modular routing system provide karta hai
jisse routes ko logically separate karke clean aur scalable
Express.js applications banayi ja sakti hain.

--------------------------------------------------
 Final One-Line Conclusion

Router ke bina app chal sakti hai,
lekin router ke saath app professional, scalable
aur real-world ready banti hai.
*/
=====================In Other Way=================
🔹 express.Router() kya hota hai?

Router = mini Express app  
- Code ko **alag-alag files** me divide karne ke liye
- Clean code, scalable project ke liye
- Same prefix ke routes ek jagah rakhne ke liye

--------------------------------------------------
🔹 Router ke bina problem kya hoti hai?

❌ Sab routes ek hi file me
❌ File bahut badi ho jati hai
❌ Maintain karna mushkil
❌ Real-world project me bad practice

Example (Router ke bina):

import express from "express";
const app = express();

app.get("/user", handler);
app.get("/user/profile", handler);
app.post("/user/login", handler);
app.delete("/user/:id", handler);

👉 Jaise-jaise project badhega, file aur gandi hoti jayegi

--------------------------------------------------
🔹 Router ke saath solution (Clean & Professional)

user.routes.js
----------------
import express from "express";
const router = express.Router();

router.get("/", (req, res) => {
  res.send("All users");
});

router.get("/profile", (req, res) => {
  res.send("User profile");
});

export default router;

server.js
----------
import express from "express";
import userRouter from "./user.routes.js";

const app = express();

app.use("/user", userRouter);

--------------------------------------------------
🔹 Router + prefix + next() ka combined dry run

Request:
GET /user/profile

Execution flow:
1) Express app.use("/user", userRouter) dekhega
2) Prefix "/user" match hua "/user/profile" se ✅
3) Control router ke paas chala gaya
4) Router ke andar "/profile" route mila
5) router.get("/profile") exact match hua
6) Response send ho gaya

Output:
User profile

--------------------------------------------------
🔹 Router use karne ke fayde (Beginner points)

1️⃣ Code clean & readable
2️⃣ Same prefix ke routes ek file me
3️⃣ Easy debugging
4️⃣ Real-world industry standard
5️⃣ Team work ke liye best
